import htmlIcon from '../functions/htmlIcon';

import svg from '../uri-encoded-icons/squat-marker-red.svg.uri';

export default function redSquatClusterMarkerIcon(message) {
    return htmlIcon(svg, message);
}
